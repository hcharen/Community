

     <h4><small>RECENT POSTS</small></h4>
     <hr>
     <%= link_to post.caption, post_path(post) %>
     <h5><span class="glyphicon glyphicon-time"></span> <%= post.created_at %></h5>
     <h5><span class="label label-danger">USERNAME</span> | <span class="label label-primary">PROFILE-BTN</span></h5><br>
     <p><%= post.text %></p>
    <%= link_to image_tag(post.image.url(:medium)), post_path(post) %>     <br><br>
